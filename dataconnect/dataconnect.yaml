specVersion: "v1beta"
serviceId: "admin-panel"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "admin-panel-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
